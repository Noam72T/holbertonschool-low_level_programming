#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
* _abs - Entry Function
* @n: random chiffre
* Print "_putchar in newline"
* Return: 0.
*/

int _abs(int n)
{
int y;
y = abs(n);
printf("%d", y);
return (0);
}
